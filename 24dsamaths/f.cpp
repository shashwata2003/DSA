#include<iostream>
#include <bits/stdc++.h>
#include <array>
using namespace std;
/*  
Pigeonhole Principle:
Given an array of N integers, the task is to find a non-empty subset such that the sum of elements of the 
subset is divisible by N. Output any such subset with its size and the indices(1-based indexing) of elements 
in the original array if it exists. 
*/
int main(){

return 0;
}